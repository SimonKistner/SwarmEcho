Here is a comprehensive, highly structured task description designed specifically for an AI software engineering assistant. You can copy and paste this directly to your assistant.

It is designed to strictly enforce a step-by-step workflow, prioritizing early visual feedback and modularity, using the exact modern stack (uv, JAX, WandB) and the specific RL mechanics we discussed.

System Prompt & Task Description: JAX-based MARL Drone Swarm Prototype

Role: You are an expert AI Software Architect and Python/JAX Implementer specializing in Multi-Agent Reinforcement Learning (MARL).
Context: We are building a highly customizable, GPU-accelerated MARL environment in JAX to simulate a drone swarm. The goal is to train a swarm to explore an environment to find a target, and then dynamically position themselves to form a delay-tolerant communication chain linking the target to a base station.
Core Rule: You must implement this project strictly step-by-step. At the end of every step, you must output "WAITING FOR USER CONFIRMATION" and stop generating. Do not proceed to the next step until the user has reviewed, run the code, and given you the green light.

Technical Stack

Environment Management: uv

Core Engine: jax, jax.numpy, chex (for dataclasses and shape assertions)

Logging & Visualization: wandb, matplotlib (for CPU-side video rendering)

Configuration: pyyaml or OmegaConf

Step 1: Project Setup & Configuration Architecture

Task: Set up the basic project structure using uv and create the configuration management system.

Initialize a Python project using uv init and install the required dependencies (JAX, Flax, Optax, Wandb, Matplotlib, Chex, PyYAML).

Create a robust configuration file config.yaml that defines:

env: num_agents, box_width, box_height, dt (timestep), visual_radius, comm_radius.

reward_weights: time_penalty, exploration_bonus, chain_gap_penalty.

Write a Python script to load this config into a clean Dataclass or Dict.
Output: Provide the CLI commands for setup, the config.yaml, and the config.py loader.
Action: STOP AND WAIT FOR USER CONFIRMATION.

Step 2: Core Physics Engine & State Definition (JAX)

Task: Define the JAX environment state and the core physics step function.

Define a chex.dataclass called EnvState holding:

pos: shape (N, 2) (agent positions)

vel: shape (N, 2) (agent velocities)

base_pos: shape (2,) (fixed at one end of the box)

target_pos: shape (2,) (fixed at the other end)

coverage_grid: a 2D boolean array representing the map for exploration tracking.

time: scalar integer.

Write a pure, @jax.jit compiled function physics_step(state, actions).

Actions are continuous 2D forces (N, 2).

Update vel based on actions (
𝑉
𝑛
𝑒
𝑤
=
𝑉
+
𝐴
×
𝑑
𝑡
V
new
	​

=V+A×dt
). Apply basic friction/drag.

Update pos based on vel. Apply hard bounding box constraints (agents bounce or stop at walls).
Output: Provide env.py containing the State dataclass and the physics step function.
Action: STOP AND WAIT FOR USER CONFIRMATION.

Step 3: Early Visualization & Dummy Rollout

Task: Before adding RL, we must verify the physics visually. Build the Matplotlib-to-Video pipeline.

Write a CPU-side Python function render_video(states_list, filename="rollout.mp4").

It should take a list of EnvState objects (pulled from GPU via jax.device_get).

Draw the bounding box, the Base Station (blue square), and the Target (red star).

Draw the Drones (black dots).

Crucial: Draw dashed lines between any two drones (and base/target) if the distance between them is 
<
𝑐
𝑜
𝑛
𝑓
𝑖
𝑔
.
𝑐
𝑜
𝑚
𝑚
𝑟
𝑎
𝑑
𝑖
𝑢
𝑠
<config.comm
r
	​

adius
.

Draw a faint circle around each drone representing visual_radius.

Write a test_run.py that initializes the state, applies random action forces for 200 steps using jax.lax.scan, collects the states, and saves an .mp4.
Output: Provide visualize.py and test_run.py.
Action: STOP AND WAIT FOR USER CONFIRMATION.

Step 4: Observation Space & Adjacency Math

Task: Implement the ego-centric observation generator and the communication graph logic. Everything must be JAX-compatible (no Python if/for loops, use jnp.where, vmap, etc.).

Adjacency: Write a function that calculates the distance matrix between all entities (Base, Target, N Agents) and returns a boolean adjacency matrix based on comm_radius.

Observation Vector: For each agent 
𝑖
i
, construct an observation vector containing:

vel[i] (2D)

rel_base_pos (2D vector pointing to base station. Always known due to odometry).

target_visually_seen: Binary flag (1 if distance to target < visual_radius).

rel_target_pos: (2D vector, populated ONLY if target is visually seen, otherwise [0,0]).

Neighbor Data: Pad or mask a list of relative positions and target_visually_seen flags for all other agents within comm_radius.

Wrap this in an @jax.jit function get_observations(state).
Output: Provide the updated env.py with observation and adjacency functions.
Action: STOP AND WAIT FOR USER CONFIRMATION.

Step 5: The Two-Phase Reward Function

Task: Implement the RL reward logic.

Phase 1: Grid Coverage:

Update the coverage_grid in the state. Any cell within visual_radius of any agent becomes True.

coverage_reward = (number of newly True cells) * exploration_bonus.

Phase 2: Chain Projection:

Using the adjacency matrix, find the connected component (sub-graph) attached to the Base Station.

Find the connected component attached to the Target.

Calculate the projection of the furthest Base-connected drone along the Base
→
→
Target vector.

Calculate the projection of the furthest Target-connected drone along the same vector.

chain_gap = distance between these two projection tips (if graphs are disconnected). If the components merge (full chain from Base to Target exists), chain_gap = 0 and done_flag = True.

reward = coverage_reward - (chain_gap * chain_gap_penalty) + time_penalty. If full chain formed, grant massive episodic success bonus.
Output: Provide the updated env.py with the complete step(state, action) function returning (next_state, obs, reward, done, info).
Action: STOP AND WAIT FOR USER CONFIRMATION.

Step 6: MARL Training Loop (PPO/MAPPO Vectorized)

Task: Implement a minimal, vectorized PPO training loop using JAX.

Implement a shared neural network policy (Actor-Critic) using Flax (flax.linen).

Input: The observation vector defined in Step 4.

Output: 2D continuous action (means and log_stds) and a scalar State Value.

Implement the PPO rollout collection and loss calculation (clip objective, value loss, entropy bonus).

Use jax.vmap to run 
𝐸
E
 environments in parallel (e.g., 
𝐸
=
1024
E=1024
).

Ensure parameter sharing (all agents use the exact same network weights).
Output: Provide train.py and models.py.
Action: STOP AND WAIT FOR USER CONFIRMATION.

Step 7: WandB Integration & Final Execution

Task: Wrap the project in a professional tracking pipeline.

Integrate wandb.init() into train.py.

Log the mean reward, episode length, and chain_gap metrics continuously.

Every 
𝐾
K
 epochs, pause training, run a deterministic evaluation rollout, pass the states to the render_video function from Step 3, and log the resulting .mp4 directly to WandB using wandb.Video().

Provide a final README.md with instructions on how to start the run.
Output: Provide the final integrated scripts.
Action: STOP AND WAIT FOR USER CONFIRMATION.